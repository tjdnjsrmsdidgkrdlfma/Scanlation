# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
venv/
venv-vlm/
env/

# Local secrets (compose reads .env for ${SCANLATION_AUTH_TOKEN} etc.)
.env

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Runtime volumes + model weights (big binaries — never commit)
packages/scanlation-server/data/
packages/scanlation-server/models/
packages/scanlation-server/crops/
packages/scanlation-server/tools/_mit_weights/
*.onnx
*.sqlite
*.sqlite-wal
*.sqlite-shm

# Local test inputs (copyrighted manga scans — keep out of the public repo)
packages/scanlation-server/samples/

# Regenerable tool outputs — compare_models.py writes everything under compare_out/
packages/scanlation-server/compare_out/
# visualize.py single-image output
*.annotated.png
annotated.png

# Local tooling / editor config
.claude/

# OS / editor
.DS_Store
Thumbs.db

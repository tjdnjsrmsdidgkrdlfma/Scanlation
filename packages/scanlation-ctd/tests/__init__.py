"""Tests for scanlation-ctd. Self-contained: they instantiate CTDDetector /
decode directly (no server core), and self-skip when the ONNX weights are
absent. Run: python -m tests (from packages/scanlation-ctd/)."""

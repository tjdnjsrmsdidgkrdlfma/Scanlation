"""Vendored 48px / 48px_ctc OCR model code from zyddnys/manga-image-translator
(GPL). Research-only — used by tools/compare_models.py for the OCR bake-off,
never by the server product code. Weights live alongside in _mit_weights/.
"""
